const CONFIG = {
  OPENROUTER_API_KEY: "YOUR_OPENROUTER_API_KEY", // Get a free key at openrouter.ai
  UMAMI_WEBSITE_ID: "YOUR_UMAMI_WEBSITE_ID", // Get the website ID free at umami.is
  UMAMI_PUBLIC_KEY: "YOUR_UMAMI_PUBLIC_KEY", // Get the read-only public key free at umami.is
  GOATCOUNTER_CODE: "YOUR_GOATCOUNTER_CODE", // Get your free subdomain code at goatcounter.com
  GITHUB_REPO: "username/humanos" // Use your GitHub repo in the format "username/humanos"
};

window.CONFIG = CONFIG;
